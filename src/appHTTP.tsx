import React from 'react';
import { render } from 'react-dom';

const $root = document.getElementById('root');

const App = () => {
    return(
        <div>
            <h1>todo certo</h1>
        </div>
    )
}

render(<App/>, $root)